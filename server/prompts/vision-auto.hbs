请分析这张图片，自动识别其属于哪种金融信息类型，并提取关键数据进行分析。

1. **内容分类**：判断图片类型（K线图、持仓单、交割单、个股详情、新闻研报、或其他）
2. **核心数据**：提取图中可见的全部关键数值
3. **关键信息**：提取股票名称、代码、时间等标识性信息
4. **智能分析**：基于识别到的内容，给出完整的分析和建议

请严格按以下 JSON 格式返回，**`analysis` 字段必须包含 200 字以上的完整分析**：
```json
{
  "type": "kline | portfolio | detail | news | other",
  "summary": "一句话总结图片内容",
  "data": { "关键字段": "数值" },
  "analysis": "在这里写 200 字以上的完整分析内容，根据识别到的图片类型进行针对性的深度分析。不要写'详细分析见下文'之类的占位符。"
}
```
